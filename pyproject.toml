[project]
name = "kv-cache-hijack"
version = "0.1.0"
description = "Environment and dependency configuration for KV Cache Hijack."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "accelerate==1.12.0",
    "matplotlib==3.10.8",
    "torch==2.10.0",
    "tqdm==4.67.3",
    "transformers==5.1.0",
]

[tool.uv]
package = false
